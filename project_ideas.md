# 專案發想過程

先以文字記錄簡單構想
再請ChatGPT檢查點子的可行度 + 補充細節

[TOC]

## 技術架構

抽籤系統 🎴	
- 抽籤 + 紀錄歷史
- FastAPI / Flask + Cookie + DB
- 一人負責 API，一人負責儲存與歷史查詢



- 後端：FastAPI（Python）
- 資料庫：MySQL / PostgreSQL（存抽籤紀錄）或 SQLite（輕量）
- 前端（簡單）：HTML + JavaScript（使用 Fetch API 呼叫後端）
- 身份識別：使用 Cookie（儲存一個隨機 UUID，讓使用者不用登入也能查看歷史紀錄）
- 基本 API 設計:
    - 抽籤 (POST /draw)
    - 輸入：無
    - 輸出：{ "result": "大吉", "poem": "風起雲湧，萬事如意。" }
    - 後端將結果存入資料庫
    - 查詢紀錄 (GET /history)
    - 根據 Cookie 內的 UUID，查詢該使用者的所有抽籤紀錄

> 抽籤程式功能加強與新功能發想
> 基於原本的 「抽籤 + 記錄歷史」，我們可以擴展更多有趣的功能：
> 🔹 基礎功能（原本的功能）
> ✅ 抽籤功能（隨機回傳「大吉、吉、小吉、凶」）
> ✅ 記錄抽籤結果（日期、時間、結果）
> ✅ 使用 Cookie 來區別使用者
> 🔹 進階功能
> 🔹 自訂籤詩內容：允許管理員或使用者提交自訂籤詩庫，讓內容更豐富
> 🔹 每日限抽 1 次：讓運勢更具儀式感，使用 Session 控制每天只能抽一次
> 🔹 歷史趨勢分析：統計個人運勢趨勢（顯示過去 7 天的抽籤結果）
> 🔹 抽籤排行榜：計算大家的運勢走勢，顯示「運勢最佳 / 最衰排行榜」
> 🔹 進一步擴充
> 🔹 籤詩解析：每個籤詩額外附帶詳細解釋（例如愛情運、事業運）
> 🔹 分享功能：允許使用者把自己的籤詩分享到社群媒體
> 🔹 活動模式：可以有「新年特別籤」、「戀愛運勢專屬籤」等特殊活動
> 🔹 神秘獎勵機制：例如連續抽到大吉 3 次，獲得特殊訊息
> 這樣的擴展不但 保留原本的簡單性，還能讓專案變得 更有趣、更有深度，也讓你們在合作時有更多可以負責的部分！😃


## 新功能發想


### 「一起抽籤」

現實中的抽籤 偶爾會看到一組人(大約2~6)人一起抽然後綁在繩子上
所以我覺得可以寫這個:
- 多人組隊
- 建立抽籤場地
- 建立抽籤箱子
- "一起抽籤" 所以同一個筒子的籤不應該重複


因為我覺得我不太會選名詞所以叫ChatGPT幫忙選
最後是像這樣
- 籤場: 組隊畫面(類似遊戲裡的隊伍大廳,房間等概念)
- 籤筒: 一起抽籤的箱子

📝 功能設計
1️⃣ 主要概念
- 創建房間：一個人可以創建一個「抽籤房間」，獲得 分享網址，其他人點擊連結即可加入。
- 共同抽籤：所有參與者在同一時間內（例如 5 分鐘內）抽籤，每個人的結果都會顯示在頁面上。
- 結果展示：抽籤結束後，大家可以看到所有人的籤詩結果，並允許下載或分享截圖。
🛠️ 實作方法
1️⃣ 建立房間
- 使用者點擊「建立抽籤房間」，後端會：
- 生成一個 唯一的房間 ID（UUID 或短碼，例如 https://example.com/draw/abcd1234）
- 記錄這個房間的創建時間（設定 5 分鐘有效期）
- 返回這個分享網址給使用者
2️⃣ 加入房間
- 其他使用者點擊連結時，後端會：
- 確認這個 房間 ID 是否有效（檢查是否過期）
- 讓新使用者加入該房間，並記錄他的 Cookie 或 session ID
3️⃣ 抽籤邏輯
當使用者按下「抽籤」：
- 後端隨機生成籤詩結果
- 把結果儲存到資料庫，並關聯到該 房間 ID
4️⃣ 結果顯示
- 當 所有參與者抽完 或 時間到達 5 分鐘：
- 後端統計所有人的結果，回傳一個結果頁面
- 讓大家可以分享截圖或下載結果
5️⃣ 自動清理過期房間
- 設計一個 背景任務（cron job），定期清除超過 5 分鐘 的過期房間，避免佔用資料庫空間。

🔧 技術細節
|功能 |技術選擇|    
|--|--|
|唯一房間 ID| UUID 或短碼（如 abcd1234）|
|使用者識別	|Cookie / Session ID|
|即時同步（選擇性）	|WebSocket（顯示大家的即時結果）或輪詢 API|
|儲存結果	|SQLite / PostgreSQL|
|清理過期房間	|定時任務（Celery / Cron Job）|

這個功能的好處是：
✅ 增強互動性，讓多人一起參與抽籤，增加樂趣
✅ 技術上不難，主要是房間管理 + 簡單的抽獎邏輯
✅ 可擴展，未來可以加入「語音祝福」「歷史統計」等功能

### 🛠️ 短時間內「同箱不重複」抽籤的實作方法

#### 1️⃣ 設計籤筒（虛擬箱子）
- 當使用者 **建立籤場**，後端會：
  - 生成一個籤筒（例如 `list籤池`）  
  - 依照「大吉、吉、小吉、凶」的比例，隨機填入 N 個籤  
  - 儲存這個籤筒到 **資料庫或記憶體（Redis）**  

#### 2️⃣ 使用者抽籤
- 當某個使用者在這個籤場內 **按下抽籤**，後端會：
  - 從該籤場的籤筒 **隨機抽出 1 張籤**  
  - **將該籤從籤筒中移除**（確保其他人不會抽到相同的）  
  - 回傳抽中的籤給使用者  

#### 3️⃣ 所有籤抽完後的處理
- 如果籤筒內的籤被抽完：
  - **選項 1（重置）**：補充新一輪籤，讓後來的人繼續抽  
  - **選項 2（固定數量）**：該籤場的籤被抽完後，顯示「已抽完，請稍後再試」  

#### 4️⃣ 避免不同籤場的籤干擾
- **每個籤場的籤筒** 都是獨立的，所以不同籤場的籤不會互相影響  
- 這樣可以確保同一籤場內的人在 **短時間內不會重複抽到相同籤**  

---

#### 🔧 技術細節

| 功能 | 技術選擇 |
|------|--------|
| 籤筒存儲 | **Redis**（快速存取）或 **PostgreSQL** |
| 籤的隨機抽取 | `random.choice()` 或 **隨機索引** |
| 避免重複抽取 | `pop()` 移除已抽出的籤 |
| 籤筒重置 | 定時任務（Celery / Cron Job） |
| 籤場獨立性 | 每個籤場一組獨立籤筒（用 Room ID 區分） |

---

這樣的方式可以讓「多人同時抽籤」更接近現實的 **籤筒抽取機制**，讓體驗更真實！


## UX設計

為了避免漏掉什麼細節而請ChatGPT幫我順了一遍:

## 🎲 多人「一起抽籤」功能實作方法

這個設計強調 **現場感**，讓多人可以同時參與抽籤，並透過分享功能提升互動性。以下是各功能的 **實作方法**，並對按鈕名稱進行調整，讓它們更簡潔但保留儀式感。

---

### 🛠️ 1️⃣ 「一起抽籤」（建立多人抽籤場）
#### **實作方法**
- **前端**：
  - 使用 JavaScript 觸發 API 請求，向後端請求建立新的「籤場」
- **後端**：
  - 產生一個 **唯一籤場 ID**（UUID 或短碼，例如 `https://example.com/lot/abcd1234`）
  - 建立一個「籤筒」，放入 **N 支籤**
  - 記錄籤場的建立時間（例如 10 分鐘內有效）
  - 回傳「籤場網址」給前端

📌 **按鈕名稱：「一起抽籤」 →** ✅ **「開啟籤場」**  

---

### 🛠️ 2️⃣ 進入籤場
#### **實作方法**
- **前端**：
  - 顯示「正在準備籤筒...」動畫
  - 讓使用者進入後，看到「籤筒」並等待其他人加入
- **後端**：
  - 確認該「籤場」是否仍有效（檢查是否過期）
  - 回傳籤筒內還剩下的籤數，讓前端渲染

📌 **顯示訊息：「正在建立籤場」 →** ✅ **「籤場開啟，等待夥伴加入」**  
📌 **顯示訊息：「正在準備籤筒」 →** ✅ **「籤筒已備妥，請拿一支籤」**  

---

### 🛠️ 3️⃣ 「抽取籤筒」
#### **實作方法**
- **前端**：
  - 按下後，UI 顯示一支模糊的籤，等待使用者按「揭曉籤詩」
- **後端**：
  - 從「籤筒」隨機取出 **一支籤**，並從籤筒列表移除
  - 記錄這名使用者的抽籤結果

📌 **按鈕名稱：「拿一支籤」 →** ✅ **「抽取籤筒」**  

---

### 🛠️ 4️⃣ 「揭曉籤詩」
#### **實作方法**
- **前端**：
  - 顯示籤的內容（運勢 + 詩籤）
  - 允許使用者選擇是否「秀出給大家」或「分享出去」
- **後端**：
  - 這個功能不需要後端處理，只是前端 UI 更新

📌 **按鈕名稱：「打開」 →** ✅ **「揭曉籤詩」**  

---

### 🛠️ 5️⃣ 「秀出我的籤」
#### **實作方法**
- **前端**：
  - 把自己的籤結果發送到 WebSocket，讓籤場內的所有人都能看到
- **後端**：
  - 如果有 WebSocket，可以即時推送這名使用者的籤結果給其他人
  - 如果沒用 WebSocket，可以用「輪詢 API」來每 5 秒請求一次更新

📌 **按鈕名稱：「展示我的籤給一起抽籤的人」 →** ✅ **「秀出我的籤」**  

---

### 🛠️ 6️⃣ 「分享籤詩」
#### **實作方法**
- **前端**：
  - 提供社群分享連結（Facebook、LINE、Twitter 等）
  - 讓使用者複製籤詩 + 籤場網址分享給朋友

📌 **按鈕名稱：「分享我的籤」 →** ✅ **「分享籤詩」**  

---

### 🛠️ 7️⃣ 「分享籤場」
#### **實作方法**
- **前端**：
  - 允許使用者將「目前已抽出的籤 + 籤場網址」分享到社群
- **後端**：
  - 儲存「籤場內的所有籤結果」，讓分享連結能顯示完整的抽籤歷史

📌 **按鈕名稱：「分享籤場盛況」 →** ✅ **「分享籤場」**  

---

### 🔧 **技術架構建議**
| 功能 | 技術選擇 |
|------|--------|
| 籤場 ID | UUID 或短碼 (`abcd1234`) |
| 籤筒儲存 | Redis（快取）或 PostgreSQL（資料庫） |
| 即時顯示籤結果 | WebSocket（即時推送）或輪詢 API |
| 籤場清理 | 10 分鐘後自動刪除（Cron Job 或 Celery） |
| 社群分享 | 簡單分享 URL + 前端渲染 |

---

### 📌 **完整流程（按鈕名稱優化版）**
1. **開啟籤場**（建立籤場 + 籤筒）  
2. **抽取籤筒**（隨機拿一支籤）  
3. **揭曉籤詩**（顯示籤結果）  
4. **秀出我的籤**（讓籤場內其他人看到）  
5. **分享籤詩**（分享單獨的籤結果）  
6. **分享籤場**（分享整個籤場的結果）  

---

這樣的設計讓 **多人即時互動** 變得流暢，也讓籤場體驗更接近現實！🎋  

